window.__fitnessEnv = window.__fitnessEnv || {};

// Optional runtime override for production deployments where the API
// is hosted on a different origin than the Angular app.
// Example:
// window.__fitnessEnv.apiBaseUrl = 'https://api.example.com/api';
