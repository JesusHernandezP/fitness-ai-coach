package com.fitness.fitnessaicoach.presentation.auth

import androidx.compose.runtime.Composable
import com.fitness.fitnessaicoach.presentation.components.PlaceholderScreen

@Composable
fun LoginScreen() {
    PlaceholderScreen(
        title = "Login",
        description = "Authentication feature will plug into the backend auth API next."
    )
}
