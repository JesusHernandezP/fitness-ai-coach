springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://127.0.0.1:5432/fitness_db?sslmode=disable}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1234}

app:
  swagger:
    public: true

groq:
  api:
    model: llama-3.1-8b-instant
